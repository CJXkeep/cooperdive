# 复制为 .env 并填写。.env 不要提交到 git。
# 看板访问域名（需已解析到服务器 IP）
DOMAIN=dash.example.com
# HTTPS 证书通知邮箱
ACME_EMAIL=you@example.com
# 看板登录密码的 bcrypt 哈希，生成方法：
#   docker compose --profile prod run --rm caddy hash-password
#   按提示输入密码，把输出的 $2a$14$... 完整粘贴到这里
CADDY_HASH=$2a$14$REPLACE_ME
