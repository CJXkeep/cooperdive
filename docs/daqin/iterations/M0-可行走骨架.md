# M0 可行走骨架

> 迭代号：M0 | 状态：⬜ 未开始 | 关联：06 §4.1；决议 D-01、D-10、D-13、D-20、D-21、D-22
> 目标：用最短路径打通「配置 → 入库 → 指标 → 信号 → CLI」全链路，
> 让 `python -m daqin.cli signal` 在 2020 年真实数据上输出完整 S0-S5 状态序列（D 系列允许部分为 False）。
> 出口条件：Gate 0 四条命令全部通过（§4）。
> 预估：0.5-1 天（06 §4.2）。

## 1. 前置条件与待确认

| # | 事项 | 影响 | 状态 |
|---|---|---|---|
| 1 | **D-20 storage 层裁决** | 已定案：方案 A′（2026-09-21），`storage/` 基座已落地，`copper/db.py` 已改薄封装 | ✅ |
| 2 | D-22 的 `pp_avail_normal_streak` 列已补入 06 §3 DDL | M0-2 建表直接包含，无需二次改表 | ✅ |
| 3 | FRED `fredgraph.csv` 端点连通性 | 本机实测（2026-09-21）：TLS 可建连但读超时（20s），系统代理为空 → 本机直连不可达；M0-4 照常编码，数据实测待开代理或服务器；退路：手工 CSV 占位 | ⚠️ 本机不通 |

### 1.1 D-20 方案对比（✅ 已定案：方案 A′）

> 前提：两模块存储范式分叉（copper 长表 `(dataset,date,key)` vs daqin 宽表 `date` 主键）。
> 复用面评估：真正可共用的只有 `connect()`（约 10 行）与 `freshness` 读写（结构完全相同，约 40 行）；
> **upsert 无法共用**——长表是四元组 `INSERT OR REPLACE`，宽表是多列 `ON CONFLICT(date) DO UPDATE`。

| 维度 | 方案 A′：抽仓库级 `storage/` 基座 | 方案 B：两套并行 |
|---|---|---|
| 形态 | 顶层包 `storage/`（与 `copper/`、`daqin/` 平级）：`connect(path)` + freshness 读写；`copper/db.py` 改为薄封装（对外 API 不变），`daqin/storage/db.py` 复用 | `daqin/storage/db.py` 自实现 connect + freshness（约 50 行重复） |
| 收益 | freshness 语义单点维护（将来加 `last_data_date` 只改一处）；M6 单服务多页面统一展示两库健康度更顺；第三个模块零成本 | 不触碰已上线的 copper（零回归风险）；daqin 完全独立 |
| 代价 | 需机械改动 `copper/db.py`（有冒烟测试 + 幂等设计兜底）；新增一个顶层包 | 约 50 行重复；长期两处漂移风险 |
| 风险 | 低 | 中（漂移） |

**推荐：方案 A′**——复用面虽小，但 freshness 是两模块共用的运营指标（M6 要合并展示），语义一致性价值高于省下的 50 行；
copper 的改动是薄封装替换，冒烟测试可覆盖。

> ✅ **2026-09-21 已按方案 A′ 拍板落地**：`storage/` 基座建成，06 D-20 已更新为定案，M0-2 完成。

## 2. 任务清单

| # | 任务 | 出口条件 | 状态 | 实现 note |
|---|---|---|---|---|
| M0-1 | `daqin/config.py` + `daqin/thresholds.py`：YAML→dataclass + **缺键报错** | 改一个阈值立即在 `cli signal` 生效；缺键启动即报错 | ✅ | 严格加载（缺键/未知键/类型错均抛 `ThresholdsError`）、不缓存（改文件即生效）；`cli signal` 联调待 M0-7，重载语义已有单测覆盖 |
| M0-2 | `daqin/storage/db.py`：06 §3 全量 DDL + upsert + 内存库单测 | `pytest tests/daqin/test_db.py` 通过；重复写入幂等 | ✅ | 基座建于 `storage/`（D-20 A′）：11 张表 + 宽表 upsert（缺主键/列不一致显式报错）；`copper/db.py` 已改薄封装，冒烟通过 |
| M0-3 | `collectors/akshare_stock.py`：601006 + 沪深300 日线 | 2020 区间入库、日期连续 | ✅ | 实测 2020 全年 243 行、双源零缺失；**数据源由东财切换为新浪**（东财本机 RemoteDisconnected，偏差见 §5） |
| M0-4 | `collectors/fred.py`：`fredgraph.csv` 免 key（D-01） | DGS10 自 2006 起入库 | ✅ | 代码完成（"." 缺失丢弃、增量回退 30 天）；本机网络不可达 → 实测 FAIL 且 freshness 已记录（失败路径即骨架验证目标） |
| M0-5 | `indicators/align.py`：t-1 可得性对齐 + ffill，**实盘/回测共用** | 单测：宏观晚于 A 股收盘 → 取 t-1 | ✅ | `align_to_trading_days(external, trading_days, lag_days)`："观测日 + lag 天才可用"模型，防未来有单测覆盖 |
| M0-6 | `signals/rules.py` + `state_machine.py`：全部规则一次写全（含未校准阈值、D-10 降级路径、D-22 的 S5 自动核占位） | 纯函数签名符合 03 §3.4 与 D-21 | ✅ | 15 条规则 + S4/S5 条件；跳级/逐级/滞回/防抖/D-10/D-22 全实现；落地 **D-25** 补列；S3→S2 语义存疑已登记 |
| M0-7 | `daqin/cli.py`：`collect` / `compute` / `signal` | Gate 0 命令通过（§4） | ✅ | `cli.py` + `indicators/daily.py`（compute 骨架，warm-up 多算）；Gate 0 实跑见 §4.1 |

## 3. 接口契约（本迭代冻结，回测与实盘共用同一套）

```python
# daqin/config.py
DATA_DIR: Path                 # 支持 DAQIN_DATA_DIR 覆盖，容器内挂 /app/data
DB_PATH = DATA_DIR / "daqin.db"

# daqin/thresholds.py
@dataclass(frozen=True)
class Thresholds: ...
def load_thresholds(path: Path | None = None) -> Thresholds:
    """缺键/类型错 → 启动即报错；不静默取默认值（06 M0-1）。"""

# daqin/signals/rules.py —— 每条规则独立纯函数（03 §2）
def r_m1(row: pd.Series, cfg: Thresholds) -> tuple[bool, str]: ...
# 返回 (是否触发, 原因)；原因写入 signal_log.triggered_rules

# daqin/signals/state_machine.py（03 §3.4 / D-21）
def next_state(current: str, row: pd.Series, cfg: Thresholds, deb: Debouncer) -> str: ...
class Debouncer: ...
def run_daily(metrics: pd.DataFrame, cfg: Thresholds) -> pd.DataFrame:
    """逐日回放（实盘每日一次 / 回测整段调用）；纯函数，**不落库**。"""

# daqin/cli.py —— 落库职责唯一位于本层（D-21）
# collect → raw 表 + freshness；compute → metrics_daily；signal → signal_log 每日快照
```

> ⚠️ **待冻结（实现时二选一并回写 03）**：
> 1. `next_state` 签名：03 §3.4 写 `next_state(current, row, cfg)`（防抖在状态机之外），
>    06 §6 伪代码中 `deb` 参与判定（防抖在状态机之内）。防抖归属直接决定单测形态，M0-6 开工前定；
> 2. `indicators/align.py` 的具体函数签名（03 只约定职责与归属，未给签名）；
> 3. 规则返回 `(bool, reason)` 以 03 §2 为准，§6 伪代码仅为简化示意。

## 4. Gate 0 验收

```bash
python -m daqin.cli collect --names stock_daily macro_daily
python -m daqin.cli compute --start 2020-01-01 --end 2020-12-31
python -m daqin.cli signal --date 2020-03-23        # 输出状态 + 触发规则 + 仓位建议
pytest tests/daqin -q
```

通过标准：四条命令零异常；`signal` 对 2020-03-23 输出完整状态（D 系列允许全 False）；测试全绿。
**这一步失败就地改设计，不带骨架缺陷往下走（06 §4.1）。**

### 4.1 实跑结果（2026-09-21，真实数据）

| 命令 | 结果 |
|---|---|
| `collect --names stock_daily` | ✅ 5034 行（2006-08 起全历史，新浪源） |
| `collect --names macro_daily` | ❌ `ConnectionError: ReadTimeout`（FRED 本机不可达，§6 已登记）；failure 隔离与 freshness 记录正常 |
| `compute --start 2020-01-01 --end 2020-12-31` | ✅ 243 行（warm-up 多算、只写目标区间） |
| `signal --date 2020-03-23` | ✅ `状态 S3（前值 S3） 仓位 20% 触发 M4,K3` |
| `pytest tests/daqin -q` | ✅ 65 passed |

**2020 全年状态序列**：`S0 × 16 天 → 2020-02-03 起 S3 × 227 天`；唯一一次跳转触发规则为 **M4,K1,K2,K3**
（2020-02-03 与 06 §4.2 事件表"单日 -6.23%"锚点精确吻合）。
**S3 未能退出属 M0 已知边界**：S3→S4 需 K5（PB/股息率）、S3→S2 需 D 系数据，M0 均为 NULL → 待 M1 补数据复验（§6）。

## 5. 实现记录

> 只写**已实现的事实 + 为什么**（现在时；不写"计划/预计"）。
> 非直觉的选择按三段记：选择 / Alternatives（为何不选）/ 代价。
> 重要决定升格为 06 的 D-xx 并回链编号；未做完的移入 §6。

- 2026-09-20 **M0-1 完成**：`daqin/config.py`（路径常量，`DAQIN_DATA_DIR` 覆盖）+ `daqin/thresholds.py`（YAML→冻结 dataclass）。
  选择：**严格加载 + 不缓存**。Alternatives：① `lru_cache` 缓存加载结果——不选，M0-1 要求"改阈值立即生效"，YAML 读取成本可忽略；
  ② 缺键时回落代码内默认值——不选，违背「代码不得内嵌阈值」（D-11）且会掩盖拼写错误。
  代价：`thresholds.yaml` 每加一个键必须同步 dataclass，否则启动报错（刻意如此：宁可启动失败，不可静默用错值）。
  配套：`thresholds.yaml` 补 `repair.pp_avail_normal_days`（D-22 新键，初值 20 待校准）；`requirements.txt` 加 `pyyaml`。
  测试：`tests/daqin/test_thresholds.py`（8 用例）覆盖 正常加载/缺键/未知键/未知段/类型错/bool 误用/文件缺失/重载生效。
  备注：`tests/daqin/` **不放** `__init__.py`——否则 pytest 会把该目录注册为顶层 `daqin` 包、遮蔽仓库根的真包。

- 2026-09-21 **M0-2 完成**：`storage/` 基座 + `daqin/storage/db.py`（06 §3 全量 11 张表）+ `copper/db.py` 薄封装改造。
  选择：**D-20 方案 A′**——`storage/sqlite.py` 上提 connect + `FRESHNESS_DDL` + freshness 读写。Alternatives：方案 B 两套并行——不选，freshness 语义会两处漂移（M6 要合并展示两库健康度）。
  代价：改动了已上线的 `copper/db.py`；兜底是幂等设计 + 冒烟测试，本轮回归 22 用例全绿。
  配套：`Dockerfile` 增 `COPY storage ./storage`（漏掉会导致容器内 import 失败）。
  测试：`tests/storage/test_sqlite_base.py`（5 用例：建表/成功语义/失败保留 last_success/错误截断 500/排序）+
  `tests/daqin/test_db.py`（8 用例：11 表完整性 / D-22 列"DDL↔文档"防漂移断言 / 幂等覆盖 / 空输入 / 缺主键 / 列不一致 / NaN→NULL / freshness 复用）。

- 2026-09-21 **M0-3 完成**：`daqin/collectors/akshare_stock.py`（601006 + 沪深300，双源 outer join → `stock_daily`）。
  选择：**数据源改用新浪**（`stock_zh_a_daily` + `stock_zh_index_daily`）。Alternatives：① 06 §1.1 原定的东财接口（`stock_zh_a_hist` / `index_zh_a_hist`）——实测本机 `RemoteDisconnected`（远端直接断连），与 copper 的东财系接口"本机不稳、服务器正常"的历史一致；
  ② 双源 fallback——M0 骨架期不引入复杂度，待服务器复测东财后再定（见 §6）。
  代价：偏离 06 §1.1 调研结论（已在 06 §5 修订表登记待裁决）；新浪指数接口为全历史返回，需本地过滤区间（多一次全量拉取）。
  实测：2020 全年 243 行、区间 2020-01-02 ~ 12-31、daqin/csi300 缺失均为 0（临时库，未污染 `data/`）。
  测试：`tests/daqin/test_akshare_stock.py` 7 用例（列映射 / 接口改版显式报错 / 空源降级 / 指数区间过滤 / 合并幂等 / 停牌日 NULL 语义 / 增量起点）。

- 2026-09-21 **M0-4 完成**：`daqin/collectors/fred.py`（免 key `fredgraph.csv`、"." 缺失丢弃、增量回退 30 天）。
  实测本机不可达 → `collect` 优雅失败并记录 freshness——失败路径本身也是骨架要验证的内容（§4.1）。
- 2026-09-21 **M0-5 完成**：`daqin/indicators/align.py`——`align_to_trading_days(external, trading_days, lag_days=1)`。
  选择：**"可用日"模型**（观测日 + lag 自然日才可用，交易日取最近可得值）。Alternatives：`series.shift(1)`——不选，
  shift 按行平移，无法表达自然日滞后，且会把节假日缺口与"未来观测"处理错。
- 2026-09-21 **M0-6 完成**：`rules.py`（15 条规则 + `demand_bad` 滞回释放 + `s4/s5_ready`）+ `state_machine.py`
  （跳级/逐级/滞回/防抖/D-10/D-22）。实现中发现并落地 **D-25**（metrics 补 `ma60_below_streak`、`daqin_pb`）。
  契约冻结：`next_state(current, row, cfg, deb, state_days=0)` **每日恰好调用一次**（内部统一喂入全部防抖器，
  避免短路导致计数断裂）；`run_daily` 纯函数不落库（D-21）；S4/S5 的 D-10 降级按「立即」语义不经防抖。
  ⚠️ 存疑：03 §3.2 的 S3→S2「K1 复现 ∧ D 未修复」按字面实现（D 恶化时降级=加仓，语义反直觉），待 M3 复核。
- 2026-09-21 **M0-7 完成**：`cli.py`（collect/compute/signal，落库与 freshness 唯一入口）+ `indicators/daily.py`（compute 骨架）。
  选择：**compute 支持 warm-up 多算**（读 warm-up 段、只写目标区间）——否则区间前 120 个交易日的 MA120 全为 NULL。
  Gate 0 实跑结果与 2020 序列见 §4.1。

## 6. 遗留与偏差

- （M0-4 前置）FRED 端点在当前网络环境不可达：本机实测 read timeout（TLS 可建连），
  且 `git push` 同时报代理 `127.0.0.1:7890` 连接失败 → 本机国际流量依赖本地代理（clash 类），代理未运行时不通。
  待确认：① 代理开启后以 `COPPER_DISABLE_SYSTEM_PROXY=0` 运行即可取数（netutil 默认屏蔽系统代理）；
  ② 服务器直连国际线路是否可达待部署验证。两者都不阻塞 M0-4 编码：2020 数据可先用 `manual_data/` 手工 CSV 占位跑通骨架。
- （M0-3 偏差）A 股/指数数据源已由东财切至新浪（原因见 §5）。**待服务器部署时复测东财**：
  若可达，评估是否回切或保留双源；回切则同步 06 §1.1 并按 note 纪律更新本记录。
- （M0-6 存疑）03 §3.2 的 S3→S2 条件按字面实现（K1 ∧ D 未修复 = demand_bad 成立时降级），语义反直觉（加仓方向），待 M3 参数化复核。
- （待 M2/M4）S5 → S0「回补满」需仓位跟踪，M0 未实现（S5 仅能经 D-10 退回 S3）；需在 M2/M4 定义完成条件。
- （M0 边界）Gate 0 的 2020 序列停在 S3（227 天）：S3→S4 依赖 K5（PB/股息率）而 M0 无估值数据；
  M1 采集估值/股息后复验 S3→S4→S5 路径，并核对 06 §4.6 的 M4 验收标准。
