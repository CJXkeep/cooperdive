# M1 数据层补全

> 迭代号：M1 | 状态：🟡 进行中 | 关联：06 §4.3；决议 D-01、D-02、D-05、D-07、D-17、D-22、**D-26（本轮新增）**
> 目标：五类核心数据（宏观利率 / 行情估值 / 行业需求 / 公司经营 / 筹码）中 ≥4 类可持续自动入库；手工数据有导入与缺失报告。
> 出口条件：`collect` 单轮后 freshness 表中 ≥4 类数据 `last_success` 为当日；手工 CSV 导入打印行数/缺失/越界（06 §4.3）。
> 预估：1.5-2 天。

## 1. 前置条件与待确认

| # | 事项 | 影响 | 状态 |
|---|---|---|---|
| 1 | FRED 端点连通性 | M0-4 遗留：本机不可达 → 宏观数据缺失 → **M 系列在 M4 回测中无法验证** | ✅ 已解决（显式代理 + 专用请求路径，见 §5） |
| 2 | 六大电（D5 数据源） | 探活实测**停更于 2019-06-21** → D5 与 D-22 的 S5 自动核同时失效 | ✅ 已处置（**D-26**） |
| 3 | 东财公告 PDF 链路 | M1-3（最大单点工程量）：定位 → 下载 → `pdfplumber` 解析，模板稳定性未验证 | ⬜ 待实施 |
| 4 | 本机代理未恢复 | `git push` 与 FRED 同时受阻 | ⚠️ 外部依赖 |

### 1.1 数据源探活结论（2026-09-21 实测）

| 数据源 | 接口 | 结果 |
|---|---|---|
| 沿海六大电（D5） | `ak.macro_china_daily_energy()` | ❌ **停更于 2019-06-21**（2016-01-01 起 1207 行）→ 触发 **D-26** |
| ZC 动力煤主连（D6 代理） | `ak.futures_hist_em(symbol="ZC主连")` | ❌ `KeyError`（该函数无动力煤主连映射，仅具体合约如"焦煤2506"）→ 改用 `ak.futures_main_sina(symbol="ZC0")` ✅ 243 行（2020 全年） |
| 分红派息（股息率 TTM） | `ak.stock_dividend_cninfo(symbol="601006")` | ✅ 22 行（2007 起，含除权日/派息比例/实施方案说明） |
| FRED（M1-0 / M0-4） | `fredgraph.csv` 免 key | ❌ 本机不可达（M0 §6），待代理或服务器 |

## 2. 任务清单

| # | 任务 | 出口条件 | 状态 | 实现 note |
|---|---|---|---|---|
| M1-0 | FRED 宏观数据入库（M0-4 遗留） | DGS10 自 2006 起入库 | ✅ | 5184 行（2006-01-01 起，三序列）；本机需 `REPO_HTTP_PROXY` + 专用请求路径（见 §5 实验记录） |
| M1-1 | `collectors/futures.py`：ZC 主连 + 成交有效性过滤（D-05） | 序列无长段假主力；`zc_valid` 标记合理 | ✅ | 首次采集 1760 行；`zc_valid` 按 20 日均额 ×0.3 计算（2020 全年 243/243 有效）；D6 未被触发（最低偏离 -6.9%，阈值 -10%） |
| M1-2 | `collectors/energy.py`：六大电 | 字段名/起点核对 | ⬜ | **数据停更（2019-06）**：仅存历史段，2020+ 为 NULL；D5 降级为历史可选（D-26） |
| M1-3 | `collectors/company_report.py`：公告定位 → PDF 下载 → 解析运量与同比 | 解析失败留痕 + 手工 CSV 回退，不静默丢月 | ⬜ | **最大单点工程量**；依赖东财公告接口 + pdfplumber |
| M1-4 | `collectors/coal_manual.py`：手工 CSV 导入 + 缺失值报告 + 模板生成（D-02） | 打印行数/缺失/越界值，人工确认后入库 | ⬜ | 纯本地逻辑，先做 |
| M1-5 | `collectors/dividend.py`：分红派息 | 股息率 TTM 可算 | 🟡 | 采集完成（22 行全历史，`dps = 派息比例 ÷ 10`）；**TTM 计算待估值列**（不复权价 + 每股净资产，拟 D-27） |
| M1-6 | 2020 数据回填（含公司运量 12 行） | 2020 年 `company_monthly` 12 行齐全 | ⬜ | 依赖 M1-3 或手工 CSV |
| M1-7 | `requirements.txt` 增加 `pdfplumber`（pyyaml 已加；**不加 fredapi**） | 镜像可构建 | ⬜ | |

## 3. 接口契约（本迭代冻结）

```python
# 采集器统一签名（与 M0-4/5 一致；freshness 由 cli 层记录）
def collect(conn: sqlite3.Connection, start: str | None = None, end: str | None = None) -> int: ...

# daqin/indicators/daily.py（M1 扩展，待实施）
# 新增入 metrics 的列：dividend_yield_ttm、spread_daqin_us10y、daqin_pb、
#   dq_vol_yoy(+lag1/lag2/narrow_streak)、pp_available_days(+streak)、zc_close/zc_valid/zc_dev_20d
```

## 4. Gate 验收

```bash
python -m daqin.cli collect                 # 全部数据源（含手工 CSV 导入）
python -m daqin.cli compute --start 2020-01-01 --end 2020-12-31
python -m daqin.cli signal --date 2020-03-23
pytest tests/daqin -q
```

通过标准：五类数据 ≥4 类 `last_success` 为当日（手工类以导入时间计）；导入器输出缺失/越界报告。

## 5. 实现记录

> 只写**已实现的事实 + 为什么**（现在时；不写"计划/预计"）。
> 非直觉的选择按三段记：选择 / Alternatives（为何不选）/ 代价。
> 重要决定升格为 06 的 D-xx 并回链编号；未做完的移入 §6。

- 2026-09-21 **M1 探活与决议处置**：三源实测（见 §1.1）。**六大电停更**是本轮最重要的发现——
  它同时击穿 D5 与 D-22 的 S5 自动核，触发 **D-26**：S5 自动核改为「运量连续 2 月转正」（复用既有列 `dq_vol_yoy`/`lag1`，零 DDL 变更）。
  Alternatives：① 电厂数据改手工 CSV——不选，周频人工成本高且 S5 是退出型信号，延迟无妨但依赖人工不可靠；
  ② 引入 CCTD 等付费/半付费源——不选，与 D-06 的"投入产出比"原则冲突。
  代价：D 系自动核在 2020+ 实际退化为仅 D4（月频），S2/S5 的时间分辨率下降——已同步 M4 预期（D-23/D-26）。

- 2026-09-21 **M1-0 完成**：FRED 三序列入库 5184 行（2006-01-01 起）。本机访问 FRED 需两步：
  **① 显式代理解脱阀**——`netutil` 屏蔽系统代理的补丁会清空一切自动代理检测，新增环境变量 `REPO_HTTP_PROXY`
  （`http_get`/`fetch_text` 支持，不设时行为完全不变）；
  **② FRED 专用请求路径**——同一时点的实验矩阵：裸请求 + 显式代理稳定 200（3 次），
  而共享会话的各种组合（浏览器 UA / 中性 UA / 双键 proxies / Retry 适配器 / 代理 patch）全部超时或 ProxyError，
  根因未完全定位（判定为本机代理与会话配置的环境特异性交互；服务器直连环境下两者等价）。
  选择：`fred._fetch_csv_text`（最小会话 + 显式代理 + 重试）。Alternatives：继续深挖共享会话——不选，投入产出比低。
  验证：**t-1 对齐在真实数据上严格成立**——metrics 的 03-20/23/24/25 分别取到 macro 的 03-19/20/23/24（逐日对应）。
- 2026-09-21 **M1-1 完成**：`collectors/futures.py` + 指标层接入（`zc_close`/`zc_valid`/`zc_dev_20d`）。
  选择：**新浪主连 `futures_main_sina(ZC0)`**。Alternatives：06 §1.1 原定的 `futures_hist_em`——实测该函数映射表无动力煤主连
  （仅具体合约如"焦煤2506"）；`futures_main_sina` 是 copper 长期在用的主连接口。
  代价：`zc_amount` 只能用 `成交量 × 收盘价` 近似（接口不提供成交额）；`zc_valid` 为比值判定，与单位无关。
  实测：首次采集 1760 行；2020 全年有效 243/243，最低 20 日偏离 -6.9%（未触发 D6 阈值，与当年煤价平稳一致）。
- 2026-09-21 **M1-5（采集部分）完成**：`collectors/dividend.py`（巨潮 22 行全历史，`dps = 派息比例 ÷ 10`）。
  **未闭环**：股息率 TTM 需要「不复权价 + 每股净资产」，当前 `stock_daily` 只有 qfq 价——用 qfq 价算历史股息率会系统性高估
  （前复权下调历史价）。处置：下轮新增 `daqin_close_raw` 列与 bps 采集（拟 D-27）。

## 6. 遗留与偏差

- （待 M1 内解决）**M1-3（公告 PDF，最大单点工程量）链路未验证**；M1-4（手工 CSV 导入器）未开工；M1-2（六大电历史段）待实现。
- （M1-5 未闭环）股息率 TTM 待「不复权价 + 每股净资产」两列数据（拟 D-27），完成前 K5 保持不可用。
- （D-26 后续）D 系自动核在 2020+ 仅剩 D4（月频），M4 报告须量化 S2/S5 召回率（与 D-23 呼应）。
- （跨迭代）M0 遗留三项继续跟踪：S3→S2 语义待 M3 复核、S5→S0 待 M2/M4 定义、2020 序列边界待补数据复验。
